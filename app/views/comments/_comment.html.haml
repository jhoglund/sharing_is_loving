.clearfix
	%img.img-polaroid(src="#{comment.user.avatar.url}" style="width:50px;height:50px;float:left;margin-right:10px")
	= comment.body
	%br
	By
	= mail_to(comment.user.email,comment.user.name)
	on
	= l(comment.created_at, :format => :short)
%hr